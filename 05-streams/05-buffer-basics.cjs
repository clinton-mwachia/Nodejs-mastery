// Create a buffer
const buf = Buffer.from("Hello World");

console.log("🧠 Buffer:", buf);
console.log("📜 As string:", buf.toString());
console.log("📏 Length:", buf.length);
console.log("🔢 First byte:", buf[0]); // ASCII code
